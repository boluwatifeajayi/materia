"""Agent prompts, loaded at runtime.

The live strings live here, not in docs/AGENT_INSTRUCTIONS.md. The doc quotes
them and a test asserts the two agree, so neither can drift from the other.

  adjudicator.py   docs/AGENT_INSTRUCTIONS.md section 1
  reporter.py      docs/AGENT_INSTRUCTIONS.md section 2, not shipped
  baseline.py      docs/AGENT_INSTRUCTIONS.md section 3
"""
